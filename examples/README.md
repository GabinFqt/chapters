# Stagecraft examples

Copy `kubejs/` into your Minecraft instance's `kubejs/` folder (merge with existing scripts).

The sample `server_scripts/main.js` registers two runtime stages via KubeJS for local testing.
